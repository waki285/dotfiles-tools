name: CI agent_hooks

permissions:
  contents: read

on:
  push:
    branches:
      - main
    paths:
      - 'agent_hooks/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/ci-agent-hooks.yml'
  pull_request:
    paths:
      - 'agent_hooks/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/ci-agent-hooks.yml'

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: . -> target
          shared-key: rust-ci-workspace-${{ runner.os }}

      - name: Run cargo check
        run: cargo check --all-targets -p agent_hooks -p agent_hooks_claude -p agent_hooks_copilot -p agent_hooks_opencode

  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Run cargo fmt
        run: cargo fmt -p agent_hooks -p agent_hooks_claude -p agent_hooks_copilot -p agent_hooks_opencode -- --check

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: . -> target
          shared-key: rust-ci-workspace-${{ runner.os }}

      - name: Run cargo clippy
        run: cargo clippy --all-targets -p agent_hooks -p agent_hooks_claude -p agent_hooks_copilot -p agent_hooks_opencode -- -D warnings

  test:
    name: Test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: . -> target
          shared-key: rust-ci-workspace-${{ runner.os }}

      - name: Run cargo test
        run: cargo test --all-targets -p agent_hooks -p agent_hooks_claude -p agent_hooks_copilot -p agent_hooks_opencode

  build:
    name: Build
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: . -> target
          shared-key: rust-ci-workspace-${{ runner.os }}

      - name: Run cargo build
        run: cargo build --release -p agent_hooks -p agent_hooks_claude -p agent_hooks_copilot -p agent_hooks_opencode
